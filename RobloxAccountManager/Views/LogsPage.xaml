<Page x:Class="RobloxAccountManager.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Application Logs" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

        <ui:Card Grid.Row="1" VerticalAlignment="Stretch" Padding="0">
             <ui:TextBox 
                     VerticalAlignment="Stretch"
                     Text="{Binding LogOutput, Mode=OneWay}"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent"
                     Padding="10"
                     PlaceholderText="No logs yet..."
                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </ui:Card>
    </Grid>
</Page>
